{
  "name": "gabrial-deora-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Full-Stack Portfolio and Content Management System",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix Portfolio run dev",
    "build": "npm --prefix Portfolio run build",
    "start": "npm --prefix server start",
    "lint": "npm --prefix Portfolio run lint"
  }
}
